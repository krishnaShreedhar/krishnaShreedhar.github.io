logging:
  level: INFO
  log_dir: logs

device:
  gpu_ids: [0, 1]

training:
  max_episodes: 1000
  max_steps_per_episode: 500
  eval_interval: 100
  eval_episodes: 10
  save_interval: 200
  checkpoint_dir: checkpoints
  render_mode: headless

buffer:
  capacity: 100000
